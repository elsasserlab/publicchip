params {
    base_dir = './'
    acclist = 'acclistfile.csv'
    bw_index = 'path_to_bowtie_index'
    refdir = 'path_to_reference'
    genomesize = 2150570000
    max_threads = '12'
    binsize = 5000
}

profiles {
    standard {
        process.executor = 'local'
    }
}
